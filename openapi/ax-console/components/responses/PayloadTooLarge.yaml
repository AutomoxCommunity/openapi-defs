description: The file you are trying to upload is too large.
content:
  application/json:
    schema:
      description: ''
      type: object
      properties:
        errors:
          type: array
          items:
            type: string
      required:
        - errors
    examples:
      Payload is too large:
        value:
          errors:
            - Payload Too Large
