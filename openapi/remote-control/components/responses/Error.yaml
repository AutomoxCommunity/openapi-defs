description: Invalid Request
content:
  application/json:
    schema:
      properties:
        errors:
          type: array
          maxItems: 1
          items:
            type: string
    example:
      errors:
        - Validation Error
